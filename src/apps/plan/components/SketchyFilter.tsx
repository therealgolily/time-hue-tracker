/**
 * Swiss/International style — no sketchy filter needed, but keep component
 * so imports don't break. Renders nothing visible.
 */
const SketchyFilter = () => null;

export default SketchyFilter;
